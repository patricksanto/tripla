<%= form_with(model: @activity, url: trip_trip_day_activities_path(@trip, @trip_day)) do |form| %>
  <div class="bg-teal-500 shadow-xl rounded-xl px-8 pt-6 pb-8">
    <div class="mb-4">
      <%= form.label :name, "qual é a ideia ?", class: "block text-white text-sm font-bold mb-2" %>
      <%= form.text_field :name, class: "shadow appearance-none border-none rounded w-full py-2 px-3 text-blue-400  leading-tight focus:outline-none focus:shadow-outline" %>
    </div>

    <div class="mb-4">
      <%= form.label :description, "conta um pouco mais sobre...", class: "block text-white  text-sm font-bold mb-2" %>
      <%= form.text_field :description, class: "shadow appearance-none border-none rounded w-full py-2 px-3 text-blue-400  leading-tight focus:outline-none focus:shadow-outline" %>
    </div>
    <div class="flex items-center justify-between">
      <%= form.submit "add atividade", class: "bg-cyan-300 hover:bg-cyan-400 hover:shadow-md text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline" %>
    </div>
  </div>
<% end %>
